# @canarycoders/canaryllm-rag — DEPRECATED

Renamed to **[@canarycoders/ai-rag](https://www.npmjs.com/package/@canarycoders/ai-rag)**.

```bash
npm uninstall @canarycoders/canaryllm-rag && npm install @canarycoders/ai-rag
```

The `0.1.x` line is a thin re-export shim forwarding everything to `@canarycoders/ai-rag`. Update your imports when convenient.
